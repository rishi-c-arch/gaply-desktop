fastapi>=0.110
uvicorn[standard]>=0.29
anthropic>=0.40
httpx>=0.27
# Entitlement (Set 8): Supabase user-JWT verification (ES256/JWKS) + Postgres
# reads/atomic-consume. PyJWT[crypto] pulls in `cryptography` for ES256.
PyJWT[crypto]>=2.8
asyncpg>=0.29
pytest>=8.0
