{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal surface for the main window: core runtime events, native dialogs, and filesystem access restricted to the app-data directory. Nothing else is reachable from the webview.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default",
    "fs:default",
    {
      "identifier": "fs:scope",
      "allow": [{ "path": "$APPDATA" }, { "path": "$APPDATA/**" }]
    },
    "store:default",
    "deep-link:default",
    "opener:default"
  ]
}
